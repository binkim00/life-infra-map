# 2026.06.23 코드리뷰

## 1. 코드 리뷰 대상

이번 코드리뷰는 **마이페이지 프로필 수정 기능**을 대상으로 진행했습니다.

리뷰 대상 기능은 다음과 같습니다.

* 닉네임 수정
* 프로필 이미지 업로드
* 프로필 이미지 미리보기
* 마이페이지 정보 조회

## 2. 코드 변경 의도

사용자가 마이페이지에서 자신의 정보를 직접 확인하고 수정할 수 있도록 기능을 추가했습니다.

기존에는 사용자 정보 표시 중심이었다면, 이번 변경으로 닉네임 변경과 프로필 이미지 변경까지 가능하도록 개선했습니다.

## 3. 핵심 체크리스트

* 닉네임 중복 입력 시 처리가 되는지
* 프로필 이미지가 없을 때 기본 처리가 되는지
* 허용되지 않은 이미지 파일을 업로드할 수 없는지
* 수정 후 화면에 바로 반영되는지
* 로그인하지 않은 사용자가 접근할 수 없는지

## 4. 리뷰 코멘트

닉네임은 중복될 수 없는 값이므로, 중복 입력 시 사용자에게 알림이 필요합니다.

프로필 이미지는 jpg, png, gif, webp 등 허용된 확장자만 업로드되도록 처리한 점은 좋았습니다.

다만 이미지가 없는 사용자에 대한 기본 이미지 처리가 필요해 보입니다.

또한 프로필 수정 후 새로고침 없이 화면에 바로 반영되는지 확인이 필요합니다.

## 5. 다음 리뷰 대상

다음 리뷰에서는 **프론트의 지도 검색 / AI 검색 통합 흐름**을 확인할 예정입니다.

특히 백엔드에서 내려주는 `search`, `ask_clarification`, `blocked`, `out_of_scope` 응답을 화면에서 제대로 구분해 처리하는지 확인할 예정입니다.

추가로 검색 결과 목록과 지도 마커가 같은 데이터를 기준으로 표시되는지, 검색 실패나 결과 없음 상황에서 사용자 안내가 자연스럽게 나오는지도 확인할 예정입니다.

# 2026.06.24 코드리뷰

## 1. 코드 리뷰 대상

이번 코드리뷰는 **Intent Router 자연어 검색 분기 로직**을 대상으로 진행했습니다.

리뷰 대상 파일은 다음과 같습니다.

* `backend/recommendations/services/conversational_search_planner.py`
* `backend/recommendations/tests.py`

주요 확인 대상은 사용자의 자연어 입력을 검색, 되묻기, 차단, 범위 외 요청으로 올바르게 분류하는 로직입니다.

## 2. 코드 변경 의도

사용자가 입력한 문장을 더 안정적으로 해석하기 위해 Intent Router 로직과 회귀 테스트를 보강했습니다.

이번 변경으로 `카공하기 좋은 곳`, `흡연구역 찾아줘`, `어디 갈까?` 같은 실제 사용 문장에 대한 처리가 추가되었습니다.

또한 `감기약`, `계약서`, `정치 뉴스`처럼 장소 추천과 관련 없는 요청은 검색하지 않도록 처리했습니다.

## 3. 핵심 체크리스트

* 자연어 입력이 알맞은 추천 시나리오로 분류되는지
* 목적이 불명확한 요청은 바로 검색하지 않고 되묻는지
* 장소 추천과 관련 없는 요청은 범위 외 요청으로 처리되는지
* 위험하거나 부적절한 요청은 차단되는지
* 기존에 문제가 되었던 문장을 회귀 테스트로 확인하는지
* 검색어와 태그 조건이 의도와 다르게 변경되지 않는지

## 4. 리뷰 코멘트

`카공하기 좋은 곳` 같은 입력을 작업 가능한 카페 검색으로 처리한 점은 좋았습니다.

흡연구역 관련 입력도 별도 시나리오로 분리되어 있어 추천 의도가 더 명확해졌습니다.

다만 `work_cafe` 시나리오에서 사용자가 입력한 세부 검색어가 `카페`로 덮어써질 가능성이 있습니다.

예를 들어 `서면역 브런치 카페에서 작업할 곳` 같은 입력은 `브런치 카페` 의도가 유지되는 것이 더 자연스럽습니다.

또한 태그 필터링 부분에서 `if item in ALLOWED_TAGS or item` 조건은 빈 값이 아니면 대부분 통과하므로, 허용된 태그만 남기려는 의도라면 조건 수정이 필요해 보입니다.

`근처 흡연장 어디 있어?` 테스트는 `search`와 `ask_clarification`을 모두 허용하고 있어 조금 느슨합니다. 위치 정보가 있을 때와 없을 때를 나눠 테스트하면 더 안정적일 것 같습니다.

## 5. 다음 리뷰 대상

다음 리뷰에서는 **프론트의 지도 검색 / AI 검색 통합 흐름**을 확인할 예정입니다.

특히 백엔드 응답의 `action` 값에 따라 화면 처리가 제대로 나뉘는지 확인할 예정입니다.

확인할 내용은 다음과 같습니다.

* 검색 성공 / 실패 처리가 잘 되는지
* `ask_clarification`, `blocked`, `out_of_scope`, `search` 응답을 구분하는지
* 지도 마커와 추천 결과 목록이 같은 데이터를 기준으로 표시되는지
* 검색 중 로딩과 결과 없음 상태가 자연스럽게 표시되는지