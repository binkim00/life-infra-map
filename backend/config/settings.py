"""
Django settings for config project.

Generated by 'django-admin startproject' using Django 6.0.5.

For more information on this file, see
https://docs.djangoproject.com/en/6.0/topics/settings/

For the full list of settings and their values, see
https://docs.djangoproject.com/en/6.0/ref/settings/
"""
import os
import sys
from dotenv import load_dotenv
from pathlib import Path

# Build paths inside the project like this: BASE_DIR / 'subdir'.
BASE_DIR = Path(__file__).resolve().parent.parent
IS_TESTING = "test" in sys.argv

load_dotenv(BASE_DIR / ".env")


def _env_bool(name, default=False):
    value = os.getenv(name)
    if value is None:
        return default
    return value.strip().lower() in {"1", "true", "yes", "on"}


def _env_int(name, default, min_value=None, max_value=None):
    try:
        value = int(os.getenv(name, default))
    except (TypeError, ValueError):
        value = default

    if min_value is not None:
        value = max(value, min_value)
    if max_value is not None:
        value = min(value, max_value)
    return value


def _env_float(name, default, min_value=None, max_value=None):
    try:
        value = float(os.getenv(name, default))
    except (TypeError, ValueError):
        value = default
    if min_value is not None:
        value = max(value, min_value)
    if max_value is not None:
        value = min(value, max_value)
    return value


def _env_list(name, default=()):
    value = os.getenv(name)
    if value is None:
        return list(default)
    return [item.strip() for item in value.split(",") if item.strip()]


KAKAO_REST_API_KEY = os.getenv("KAKAO_REST_API_KEY")
AI_PROVIDER = (
    os.getenv("TEST_AI_PROVIDER", "gms")
    if IS_TESTING
    else os.getenv("AI_PROVIDER", "openai")
).strip().lower()
AI_WEB_SEARCH_ENABLED = _env_bool("AI_WEB_SEARCH_ENABLED", False)
AI_WEB_SEARCH_PROVIDER = os.getenv("AI_WEB_SEARCH_PROVIDER", "naver_search").strip().lower()
AI_WEB_SEARCH_SUMMARY_PROVIDER = os.getenv("AI_WEB_SEARCH_SUMMARY_PROVIDER", "").strip().lower()
AI_WEB_SEARCH_GROUNDING_SUPPORTED = _env_bool("AI_WEB_SEARCH_GROUNDING_SUPPORTED", False)
AI_INTENT_MODEL = os.getenv("AI_INTENT_MODEL", "gpt-5-mini")
AI_QUERY_REPAIR_MODEL = os.getenv("AI_QUERY_REPAIR_MODEL", "gpt-5-nano")
AI_RERANK_MODEL = os.getenv("AI_RERANK_MODEL", "gpt-5-nano")
AI_WEB_SEARCH_MODEL = os.getenv("AI_WEB_SEARCH_MODEL", "gpt-5-nano")
AI_WEB_SEARCH_REASONING_EFFORT = os.getenv("AI_WEB_SEARCH_REASONING_EFFORT", "").strip().lower()
AI_REASONING_EFFORT = os.getenv("AI_REASONING_EFFORT", "").strip().lower()
AI_RERANK_REASONING_EFFORT = os.getenv("AI_RERANK_REASONING_EFFORT", "low").strip().lower()
# 인텐트 플래너도 reasoning 토큰 때문에 타임아웃이 나므로 effort를 지정한다.
AI_INTENT_REASONING_EFFORT = os.getenv("AI_INTENT_REASONING_EFFORT", "low").strip().lower()
AI_WEB_SEARCH_MAX_CANDIDATES = _env_int("AI_WEB_SEARCH_MAX_CANDIDATES", 1, 1, 1)
AI_WEB_SEARCH_MAX_OUTPUT_TOKENS = _env_int("AI_WEB_SEARCH_MAX_OUTPUT_TOKENS", 800, 200, 800)
WEB_EVIDENCE_SEARCH_ENABLED = _env_bool("WEB_EVIDENCE_SEARCH_ENABLED", False)
WEB_EVIDENCE_SEARCH_MODEL = os.getenv(
    "WEB_EVIDENCE_SEARCH_MODEL", AI_WEB_SEARCH_MODEL
).strip()
WEB_EVIDENCE_SEARCH_DAILY_LIMIT = _env_int(
    "WEB_EVIDENCE_SEARCH_DAILY_LIMIT", 500, 0, 5000
)
WEB_EVIDENCE_SEARCH_MAX_COST_USD = _env_float(
    "WEB_EVIDENCE_SEARCH_MAX_COST_USD", 5.0, 0, 1000
)
WEB_EVIDENCE_SEARCH_RESERVE_COST_USD = _env_float(
    "WEB_EVIDENCE_SEARCH_RESERVE_COST_USD", 0.02, 0.01, 1
)
WEB_EVIDENCE_SEARCH_MAX_QUERIES_PER_PLACE = _env_int(
    "WEB_EVIDENCE_SEARCH_MAX_QUERIES_PER_PLACE", 2, 1, 3
)
WEB_EVIDENCE_SEARCH_MAX_OUTPUT_TOKENS = _env_int(
    "WEB_EVIDENCE_SEARCH_MAX_OUTPUT_TOKENS", 800, 200, 1200
)
AI_REASON_MODEL = os.getenv("AI_REASON_MODEL", "gpt-5-nano")
GMS_MODEL = os.getenv("GMS_MODEL", AI_INTENT_MODEL)
GMS_API_KEY = os.getenv("GMS_API_KEY", "")
GMS_API_URL = os.getenv("GMS_API_URL", "")
GMS_API_BASE_URL = os.getenv("GMS_API_BASE_URL", "")
GMS_OPENAI_RESPONSES_PATH = os.getenv(
    "GMS_OPENAI_RESPONSES_PATH",
    "api.openai.com/v1/responses",
)
OPENAI_API_KEY = os.getenv("OPENAI_API_KEY", "")
TAG_ENRICHMENT_ENABLED = _env_bool("TAG_ENRICHMENT_ENABLED", False)
TAG_ENRICHMENT_PROVIDER = os.getenv("TAG_ENRICHMENT_PROVIDER", "naver_search").strip().lower()
TAG_COLLECTION_DAILY_API_LIMIT = _env_int("TAG_COLLECTION_DAILY_API_LIMIT", 25000, 1)
TAG_COLLECTION_QUOTA_PERCENT = _env_int("TAG_COLLECTION_QUOTA_PERCENT", 90, 1, 100)
TAG_COLLECTION_DAILY_PLACE_LIMIT = _env_int("TAG_COLLECTION_DAILY_PLACE_LIMIT", 50, 1)
TAG_COLLECTION_REVISIT_DAYS = _env_int("TAG_COLLECTION_REVISIT_DAYS", 90, 1)
TAG_COLLECTION_WORKER_BATCH_SIZE = _env_int("TAG_COLLECTION_WORKER_BATCH_SIZE", 10, 1, 100)
TAG_COLLECTION_POLL_SECONDS = _env_int("TAG_COLLECTION_POLL_SECONDS", 30, 1, 3600)
TAG_COLLECTION_STALE_LOCK_MINUTES = _env_int("TAG_COLLECTION_STALE_LOCK_MINUTES", 30, 5, 1440)
TAG_COLLECTION_MODE = os.getenv("TAG_COLLECTION_MODE", "balanced").strip().lower()
TAG_COLLECTION_FOCUS_REGION = os.getenv(
    "TAG_COLLECTION_FOCUS_REGION", "부산광역시"
).strip()
TAG_COLLECTION_FOCUS_CATEGORIES = tuple(
    value.strip()
    for value in os.getenv("TAG_COLLECTION_FOCUS_CATEGORIES", "cafe,restaurant").split(",")
    if value.strip()
)
TAG_COLLECTION_BOOTSTRAP_TIER_WEIGHTS = os.getenv(
    "TAG_COLLECTION_BOOTSTRAP_TIER_WEIGHTS", "70,15,10,5"
)
TAG_COLLECTION_BOOTSTRAP_CATEGORY_MAX_SHARE = _env_int(
    "TAG_COLLECTION_BOOTSTRAP_CATEGORY_MAX_SHARE", 40, 5, 100
)
TAG_COLLECTION_BUDGET_WEIGHTS = {
    "candidate_hint": _env_int("TAG_COLLECTION_BUDGET_CANDIDATE", 45, 0, 100),
    "cafe_discovery": _env_int("TAG_COLLECTION_BUDGET_CAFE_DISCOVERY", 25, 0, 100),
    "no_tag_targeted": _env_int("TAG_COLLECTION_BUDGET_NO_TAG", 15, 0, 100),
    "high_quality_restaurant": _env_int("TAG_COLLECTION_BUDGET_RESTAURANT", 5, 0, 100),
    "stale_refresh": _env_int("TAG_COLLECTION_BUDGET_REFRESH", 1, 0, 100),
    "exploration": _env_int("TAG_COLLECTION_BUDGET_EXPLORATION", 9, 0, 100),
}
TAG_COLLECTION_CATEGORY_PRIORITIES = {
    "cafe": _env_int("TAG_COLLECTION_PRIORITY_CAFE", 20, 0, 100),
    "restaurant": _env_int("TAG_COLLECTION_PRIORITY_RESTAURANT", 20, 0, 100),
    "tourism": _env_int("TAG_COLLECTION_PRIORITY_TOURISM", 12, 0, 100),
    "city_park": _env_int("TAG_COLLECTION_PRIORITY_CITY_PARK", 12, 0, 100),
    "library": _env_int("TAG_COLLECTION_PRIORITY_LIBRARY", 18, 0, 100),
    "beach": _env_int("TAG_COLLECTION_PRIORITY_BEACH", 10, 0, 100),
    "parking": _env_int("TAG_COLLECTION_PRIORITY_PARKING", 8, 0, 100),
    "toilet": _env_int("TAG_COLLECTION_PRIORITY_TOILET", 8, 0, 100),
    "shelter": _env_int("TAG_COLLECTION_PRIORITY_SHELTER", 6, 0, 100),
}
TAG_COLLECTION_RATE_LIMIT_ENABLED = _env_bool("TAG_COLLECTION_RATE_LIMIT_ENABLED", True)
TAG_COLLECTION_NAVER_RPS = _env_int("TAG_COLLECTION_NAVER_RPS", 5, 1, 50)
TAG_COLLECTION_DEFAULT_RPS = _env_int("TAG_COLLECTION_DEFAULT_RPS", 2, 1, 50)
TAG_COLLECTION_STRUCTURED_FIRST = _env_bool("TAG_COLLECTION_STRUCTURED_FIRST", True)
TAG_COLLECTION_AI_EXTRACTOR_ENABLED = _env_bool("TAG_COLLECTION_AI_EXTRACTOR_ENABLED", False)
TAG_COLLECTION_AI_PRIORITY_THRESHOLD = _env_int("TAG_COLLECTION_AI_PRIORITY_THRESHOLD", 80, 1, 1000)
TAG_COLLECTION_AI_MAX_CALLS_PER_PLACE = _env_int("TAG_COLLECTION_AI_MAX_CALLS_PER_PLACE", 1, 0, 10)
TAG_COLLECTION_AI_MODEL = os.getenv("TAG_COLLECTION_AI_MODEL", "gpt-5-nano").strip()
TAG_COLLECTION_AI_DAILY_LIMIT = _env_int("TAG_COLLECTION_AI_DAILY_LIMIT", 50, 0, 10000)
TAG_COLLECTION_AI_MIN_IDENTITY_SCORE = _env_int("TAG_COLLECTION_AI_MIN_IDENTITY_SCORE", 70, 0, 100)
TAG_COLLECTION_AI_ALLOWED_TAGS = tuple(
    value.strip()
    for value in os.getenv(
        "TAG_COLLECTION_AI_ALLOWED_TAGS",
        "조용함,작업하기좋음,노트북작업,콘센트있음,무료와이파이,혼자이용좋음,혼밥좋음,분위기좋음,데이트좋음,대화하기좋음,웨이팅적음,장기체류좋음",
    ).split(",")
    if value.strip()
)
TAG_COLLECTION_ADOPTED_TARGET_CLUSTERS = tuple(
    value.strip()
    for value in os.getenv(
        "TAG_COLLECTION_ADOPTED_TARGET_CLUSTERS", "work_sparse,solo,ambience,long_stay,talk,waiting"
    ).split(",")
    if value.strip()
)
PUBLIC_PAGE_FETCH_USER_AGENT = os.getenv(
    "PUBLIC_PAGE_FETCH_USER_AGENT", "LifeInfraMapEvidenceBot/1.0"
).strip()
PUBLIC_PAGE_FETCH_TIMEOUT = _env_int("PUBLIC_PAGE_FETCH_TIMEOUT", 8, 2, 30)
PUBLIC_PAGE_FETCH_MAX_BYTES = _env_int("PUBLIC_PAGE_FETCH_MAX_BYTES", 3000000, 10000, 5000000)
PUBLIC_PAGE_FETCH_MIN_TEXT_LENGTH = _env_int("PUBLIC_PAGE_FETCH_MIN_TEXT_LENGTH", 200, 20, 5000)
PUBLIC_PAGE_FETCH_MAX_TEXT_LENGTH = _env_int("PUBLIC_PAGE_FETCH_MAX_TEXT_LENGTH", 100000, 1000, 500000)
TAG_COLLECTION_READINESS_THRESHOLDS = {
    "evidence_coverage": _env_float("TAG_COLLECTION_READY_EVIDENCE_COVERAGE", 0.20, 0, 1),
    "tag_coverage": _env_float("TAG_COLLECTION_READY_TAG_COVERAGE", 0.10, 0, 1),
    "high_confidence": _env_float("TAG_COLLECTION_READY_HIGH_CONFIDENCE", 0.40, 0, 1),
    "max_conflict": _env_float("TAG_COLLECTION_READY_MAX_CONFLICT", 0.10, 0, 1),
    "max_stale": _env_float("TAG_COLLECTION_READY_MAX_STALE", 0.40, 0, 1),
}
OPENAI_API_BASE_URL = os.getenv("OPENAI_API_BASE_URL", "https://api.openai.com/v1")
AI_PROVIDER_CONFIGURED = (
    (
        AI_PROVIDER == "gms"
        and bool(GMS_API_KEY)
        and bool(GMS_API_URL or GMS_API_BASE_URL)
    )
    or (
        AI_PROVIDER == "openai"
        and bool(OPENAI_API_KEY)
    )
)
CONVERSATIONAL_SEARCH_AI_ENABLED = _env_bool(
    "CONVERSATIONAL_SEARCH_AI_ENABLED",
    (not IS_TESTING and AI_PROVIDER_CONFIGURED),
)
AI_RERANK_ENABLED = (
    True
    if IS_TESTING
    else _env_bool("AI_RERANK_ENABLED", CONVERSATIONAL_SEARCH_AI_ENABLED)
)
AI_SEARCH_INTENT_MAX_ATTEMPTS = _env_int("AI_SEARCH_INTENT_MAX_ATTEMPTS", 1, 1, 2)
AI_SEARCH_PRIMARY_QUERY_LIMIT = _env_int("AI_SEARCH_PRIMARY_QUERY_LIMIT", 4, 1, 5)
AI_SEARCH_KAKAO_MAX_QUERIES = _env_int("AI_SEARCH_KAKAO_MAX_QUERIES", 3, 1, 5)
AI_SEARCH_KAKAO_QUERY_SIZE = _env_int("AI_SEARCH_KAKAO_QUERY_SIZE", 15, 1, 15)
AI_SEARCH_KAKAO_RADIUS = _env_int("AI_SEARCH_KAKAO_RADIUS", 5000, 300, 20000)
AI_SEARCH_WEB_MAX_QUERIES = _env_int("AI_SEARCH_WEB_MAX_QUERIES", 2, 1, 3)
AI_SEARCH_MIN_STRONG_MEDIUM_CANDIDATES = _env_int("AI_SEARCH_MIN_STRONG_MEDIUM_CANDIDATES", 3, 0, 20)
AI_SEARCH_MIN_RESULTS = _env_int("AI_SEARCH_MIN_RESULTS", 10, 1, 20)
AI_SEARCH_RERANK_MAX_CANDIDATES = _env_int("AI_SEARCH_RERANK_MAX_CANDIDATES", 8, 5, 30)
AI_SEARCH_RERANK_MAX_COMPLETION_TOKENS = _env_int("AI_SEARCH_RERANK_MAX_COMPLETION_TOKENS", 2500, 800, 4000)
AI_SEARCH_HYBRID_WEIGHTS = {
    "condition": _env_float("AI_SEARCH_WEIGHT_CONDITION", 0.20, 0, 1),
    "tag": _env_float("AI_SEARCH_WEIGHT_TAG", 0.10, 0, 1),
    "semantic": _env_float("AI_SEARCH_WEIGHT_SEMANTIC", 0.35, 0, 1),
    "distance": _env_float("AI_SEARCH_WEIGHT_DISTANCE", 0.10, 0, 1),
    "evidence": _env_float("AI_SEARCH_WEIGHT_EVIDENCE", 0.10, 0, 1),
    "freshness": _env_float("AI_SEARCH_WEIGHT_FRESHNESS", 0.05, 0, 1),
    "reliability": _env_float("AI_SEARCH_WEIGHT_RELIABILITY", 0.10, 0, 1),
}
SEMANTIC_RETRIEVAL_ENABLED = _env_bool("SEMANTIC_RETRIEVAL_ENABLED", False)
SEMANTIC_EMBEDDING_PROVIDER = os.getenv("SEMANTIC_EMBEDDING_PROVIDER", "").strip().lower()
SEMANTIC_EMBEDDING_MODEL = os.getenv("SEMANTIC_EMBEDDING_MODEL", "text-embedding-3-small").strip()
SEMANTIC_EMBEDDING_DIMENSIONS = _env_int("SEMANTIC_EMBEDDING_DIMENSIONS", 512, 128, 1536)
SEMANTIC_DOCUMENT_STRATEGY = os.getenv("SEMANTIC_DOCUMENT_STRATEGY", "contextual").strip().lower()
SEMANTIC_PILOT_MAX_DOCUMENTS = _env_int("SEMANTIC_PILOT_MAX_DOCUMENTS", 10000, 1, 10000)
SEMANTIC_CANDIDATE_INJECTION_ENABLED = _env_bool("SEMANTIC_CANDIDATE_INJECTION_ENABLED", False)
SEMANTIC_TOP_K = _env_int("SEMANTIC_TOP_K", 10, 1, 50)
SEMANTIC_CANDIDATE_LIMIT = _env_int("SEMANTIC_CANDIDATE_LIMIT", 5, 1, 20)
SEMANTIC_RETRIEVAL_WEIGHT = _env_float("SEMANTIC_RETRIEVAL_WEIGHT", 0.10, 0, 1)
SEMANTIC_PGVECTOR_DSN = os.getenv("SEMANTIC_PGVECTOR_DSN", "").strip()
SEMANTIC_QUERY_EMBEDDING_CACHE_TTL = _env_int("SEMANTIC_QUERY_EMBEDDING_CACHE_TTL", 900, 0, 86400)
SEMANTIC_QUERY_EMBEDDING_CACHE_VERSION = os.getenv(
    "SEMANTIC_QUERY_EMBEDDING_CACHE_VERSION", "v1"
).strip()
# 같은 후보 묶음에 대한 AI 판정 재사용 시간(초). 0이면 캐시하지 않는다.
# 테스트는 각 케이스가 실제 호출 경로를 그대로 타야 하므로 캐시를 쓰지 않는다.
AI_RERANK_CACHE_TTL = (
    0
    if IS_TESTING
    else _env_int("AI_RERANK_CACHE_TTL", 900, 0, 86400)
)
# 화장실/주차장처럼 카테고리와 거리로 답이 정해지는 요청에서 AI 후보 평가를 건너뛴다.
AI_SEARCH_ROUTE_CATEGORY_REQUESTS = _env_bool("AI_SEARCH_ROUTE_CATEGORY_REQUESTS", True)
AI_REQUEST_TIMEOUT = int(os.getenv("AI_REQUEST_TIMEOUT", "20"))
AI_INTENT_TIMEOUT = _env_int("AI_INTENT_TIMEOUT", min(AI_REQUEST_TIMEOUT, 10), 3, 30)
AI_QUERY_REPAIR_TIMEOUT = _env_int("AI_QUERY_REPAIR_TIMEOUT", min(AI_REQUEST_TIMEOUT, 5), 2, 20)
AI_RERANK_TIMEOUT = _env_int("AI_RERANK_TIMEOUT", max(AI_REQUEST_TIMEOUT, 20), 2, 40)
NAVER_SEARCH_CLIENT_ID = os.getenv("NAVER_SEARCH_CLIENT_ID", "")
NAVER_SEARCH_CLIENT_SECRET = os.getenv("NAVER_SEARCH_CLIENT_SECRET", "")
NAVER_SEARCH_DISPLAY = _env_int("NAVER_SEARCH_DISPLAY", 5, 1, 100)
NAVER_SEARCH_SORT = os.getenv("NAVER_SEARCH_SORT", "sim").strip() or "sim"
AI_WEB_SEARCH_AVAILABLE = (
    AI_WEB_SEARCH_ENABLED
    and (
        (
            AI_WEB_SEARCH_PROVIDER == "gms"
            and bool(GMS_API_KEY)
            and bool(GMS_API_URL or GMS_API_BASE_URL)
        )
        or (
            AI_WEB_SEARCH_PROVIDER == "openai"
            and bool(OPENAI_API_KEY)
        )
        or AI_WEB_SEARCH_PROVIDER == "naver_search"
    )
)
AI_WEB_SEARCH_AUTO_MERGE_ENABLED = _env_bool("AI_WEB_SEARCH_AUTO_MERGE_ENABLED", AI_WEB_SEARCH_AVAILABLE)

# Quick-start development settings - unsuitable for production
# See https://docs.djangoproject.com/en/6.0/howto/deployment/checklist/

# SECURITY WARNING: keep the secret key used in production secret!
SECRET_KEY = os.getenv(
    "DJANGO_SECRET_KEY",
    'django-insecure-@$$oq#1_&d5ym20rfjx(c#t3f0hkyext842=2kk7nnsu8jim*$',
)

# SECURITY WARNING: don't run with debug turned on in production!
DEBUG = _env_bool("DJANGO_DEBUG", True)

ALLOWED_HOSTS = _env_list("DJANGO_ALLOWED_HOSTS", [])


# Application definition

INSTALLED_APPS = [
    # 기존 앱들
    'django.contrib.admin',
    'django.contrib.auth',
    'django.contrib.contenttypes',
    'django.contrib.sessions',
    'django.contrib.messages',
    'django.contrib.staticfiles',

    # 외부 라이브러리
    'rest_framework',
    'rest_framework.authtoken',
    'corsheaders',

    # 로컬 앱
    'recommendations',
    'accounts',
    'boards',
]

MIDDLEWARE = [
    'corsheaders.middleware.CorsMiddleware',

    'django.middleware.security.SecurityMiddleware',
    'django.contrib.sessions.middleware.SessionMiddleware',
    'django.middleware.common.CommonMiddleware',
    'django.middleware.csrf.CsrfViewMiddleware',
    'django.contrib.auth.middleware.AuthenticationMiddleware',
    'django.contrib.messages.middleware.MessageMiddleware',
    'django.middleware.clickjacking.XFrameOptionsMiddleware',
]

ROOT_URLCONF = 'config.urls'

TEMPLATES = [
    {
        'BACKEND': 'django.template.backends.django.DjangoTemplates',
        'DIRS': [],
        'APP_DIRS': True,
        'OPTIONS': {
            'context_processors': [
                'django.template.context_processors.request',
                'django.contrib.auth.context_processors.auth',
                'django.contrib.messages.context_processors.messages',
            ],
        },
    },
]

WSGI_APPLICATION = 'config.wsgi.application'


# Database
# https://docs.djangoproject.com/en/6.0/ref/settings/#databases

# Django와 Spring은 하나의 PostgreSQL/PostGIS 데이터베이스를 공유합니다.
DATABASES = {
    "default": {
        "ENGINE": "django.db.backends.postgresql",
        "NAME": os.getenv("POSTGRES_DB", "life_infra_map"),
        "USER": os.getenv("POSTGRES_USER", "life_infra_map"),
        "PASSWORD": os.getenv("POSTGRES_PASSWORD", "life_infra_map"),
        "HOST": os.getenv("POSTGRES_HOST", "127.0.0.1"),
        "PORT": os.getenv("POSTGRES_PORT", "5432"),
        "CONN_MAX_AGE": _env_int("DB_CONN_MAX_AGE", 60, 0, 600),
        "OPTIONS": {
            "connect_timeout": _env_int("DB_CONNECT_TIMEOUT", 5, 1, 30),
        },
    }
}


# Password validation
# https://docs.djangoproject.com/en/6.0/ref/settings/#auth-password-validators

AUTH_PASSWORD_VALIDATORS = [
    {
        'NAME': 'django.contrib.auth.password_validation.UserAttributeSimilarityValidator',
    },
    {
        'NAME': 'django.contrib.auth.password_validation.MinimumLengthValidator',
    },
    {
        'NAME': 'django.contrib.auth.password_validation.CommonPasswordValidator',
    },
    {
        'NAME': 'django.contrib.auth.password_validation.NumericPasswordValidator',
    },
]


# Internationalization
# https://docs.djangoproject.com/en/6.0/topics/i18n/

LANGUAGE_CODE = 'ko-kr'

TIME_ZONE = 'Asia/Seoul'

USE_I18N = True

USE_TZ = True


# Static files (CSS, JavaScript, Images)
# https://docs.djangoproject.com/en/6.0/howto/static-files/

STATIC_URL = 'static/'

MEDIA_URL = '/media/'
MEDIA_ROOT = BASE_DIR / 'media'

# 업로드 파일 저장 위치입니다.
# `local` 은 기존처럼 backend/media/ 에 두고, `s3` 는 S3 호환 저장소(MinIO/AWS S3)에 둡니다.
# 배포하면 컨테이너 재시작마다 로컬 파일이 사라지므로 s3 를 씁니다.
FILE_STORAGE_BACKEND = os.getenv("FILE_STORAGE_BACKEND", "local").strip().lower()

# 테스트는 저장소 컨테이너가 떠 있는지에 좌우되면 안 되고,
# 실행할 때마다 버킷에 파일을 남겨서도 안 됩니다.
if IS_TESTING:
    FILE_STORAGE_BACKEND = "local"

if FILE_STORAGE_BACKEND == "s3":
    AWS_ACCESS_KEY_ID = os.getenv("S3_ACCESS_KEY", "")
    AWS_SECRET_ACCESS_KEY = os.getenv("S3_SECRET_KEY", "")
    AWS_STORAGE_BUCKET_NAME = os.getenv("S3_BUCKET", "life-infra-map-media")
    AWS_S3_REGION_NAME = os.getenv("S3_REGION", "us-east-1")
    # MinIO 를 쓸 때만 지정합니다. 실제 AWS S3 로 가면 비워 두면 됩니다.
    AWS_S3_ENDPOINT_URL = os.getenv("S3_ENDPOINT_URL", "") or None
    # 브라우저가 접근할 주소입니다. 컨테이너 내부 주소와 다를 수 있어 따로 둡니다.
    AWS_S3_CUSTOM_DOMAIN = os.getenv("S3_PUBLIC_DOMAIN", "") or None
    # 로컬 MinIO 는 http 이고 배포 S3 는 https 입니다.
    AWS_S3_URL_PROTOCOL = os.getenv("S3_URL_PROTOCOL", "https:").strip()
    AWS_QUERYSTRING_AUTH = _env_bool("S3_QUERYSTRING_AUTH", False)
    AWS_S3_FILE_OVERWRITE = False
    AWS_DEFAULT_ACL = None
    # MinIO 는 가상 호스트 방식 주소를 기본 지원하지 않습니다.
    AWS_S3_ADDRESSING_STYLE = os.getenv("S3_ADDRESSING_STYLE", "path")

    STORAGES = {
        "default": {"BACKEND": "storages.backends.s3.S3Storage"},
        "staticfiles": {"BACKEND": "django.contrib.staticfiles.storage.StaticFilesStorage"},
    }

# CORS 설정
CORS_ALLOWED_ORIGINS = _env_list("DJANGO_CORS_ALLOWED_ORIGINS", [
    'http://localhost:5173',
    'http://127.0.0.1:5173',
    'http://localhost:8081',
    'http://127.0.0.1:8081',
    'http://localhost:8082',
    'http://127.0.0.1:8082',
])
CSRF_TRUSTED_ORIGINS = _env_list("DJANGO_CSRF_TRUSTED_ORIGINS", [])

# DRF 설정
REST_FRAMEWORK = {
    'DEFAULT_RENDERER_CLASSES': [
        'rest_framework.renderers.JSONRenderer',
        'rest_framework.renderers.BrowsableAPIRenderer',
    ],
    'DEFAULT_AUTHENTICATION_CLASSES': [
        # Spring 서비스가 발급한 토큰을 먼저 확인하고, 아니면 기존 방식으로 넘어갑니다.
        'accounts.authentication.SharedJWTAuthentication',
        'rest_framework.authentication.TokenAuthentication',
    ],
}

# 공개 검색 API는 외부 AI/검색 제공자 호출과 DB 연산을 유발하므로 사용자 또는 IP별로 제한합니다.
# 테스트는 대량의 평가 케이스를 한 프로세스에서 실행하므로 충분히 높은 값을 사용합니다.
API_AI_SEARCH_RATE = os.getenv(
    "API_AI_SEARCH_RATE", "10000/minute" if IS_TESTING else "30/minute"
)
API_EXTERNAL_SEARCH_RATE = os.getenv(
    "API_EXTERNAL_SEARCH_RATE", "10000/minute" if IS_TESTING else "8/minute"
)
API_CONVERSATION_RATE = os.getenv(
    "API_CONVERSATION_RATE", "10000/minute" if IS_TESTING else "40/minute"
)

# Spring 서비스와 공유하는 토큰 설정입니다.
# `spring-api` 의 app.jwt.secret 과 같은 값이어야 서로 발급한 토큰을 검증할 수 있습니다.
JWT_SECRET = os.getenv("JWT_SECRET", "")
JWT_ALGORITHM = os.getenv("JWT_ALGORITHM", "HS256")
JWT_ACCESS_MINUTES = _env_int("JWT_ACCESS_MINUTES", 120, 5, 1440)

DEFAULT_AUTO_FIELD = 'django.db.models.BigAutoField'


